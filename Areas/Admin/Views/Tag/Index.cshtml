@{
    Layout = "_Layout";
}

@section Title {
        Danh sách tag
}

<div class="row mb-3 align-items-center">
    <div class="col-md-6 text-end">
        <a asp-area="Admin" asp-controller="Tag" asp-action="Create" class="btn btn-success">
            <i class="bi bi-plus-circle me-1"></i> Thêm tag
        </a>
    </div>
</div>

<br />

<div id="tagList">
    <!-- Danh sách tag sẽ load ở đây -->
</div>

<nav aria-label="Page navigation">
    <ul class="pagination justify-content-center" id="pagination">
        <!-- Nút phân trang sẽ load ở đây -->
    </ul>
</nav>

@section js {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        $(document).ready(function () {
            const pageSize = 8; // Số tag mỗi trang
            let currentPage = 1;
            let tagsData = []; // Lưu tất cả tag

            // 1. Lấy dữ liệu từ API
            $.ajax({
                url: '/api/tags',
                type: 'GET',
                dataType: 'json',
                success: function (data) {
                    tagsData = data;
                    renderTable(currentPage);
                    renderPagination();
                },
                error: function (err) {
                    console.error(err);
                    $('#tagList').html('<p class="text-danger">Không tải được dữ liệu</p>');
                }
            });

            // 2. Render bảng theo trang
            function renderTable(page) {
                let start = (page - 1) * pageSize;
                let end = start + pageSize;
                let pageData = tagsData.slice(start, end);

                let html = '<table class="table table-bordered table-striped">';
                html += '<thead><tr><th>ID</th><th>Tên tag</th><th>Action</th></tr></thead><tbody>';

                pageData.forEach(t => {
                   
                    html += `<tr>
                        <td>${t.id}</td>
                        <td>${t.tagName}</td>
                        <td>
                            <a href="/Admin/Tag/Edit/${t.id}" class="btn btn-sm btn-primary">Sửa</a>
                            <a href="/Admin/Tag/Delete/${t.id}" class="btn btn-sm btn-danger">Xóa</a>
                        </td>
                    </tr>`;
                });

                html += '</tbody></table>';
                $('#tagList').html(html);
            }

            // 3. Render phân trang
            function renderPagination() {
                const totalPages = Math.ceil(tagsData.length / pageSize);
                let html = '';

                for (let i = 1; i <= totalPages; i++) {
                    html += `<li class="page-item ${i === currentPage ? 'active' : ''}">
                                <a class="page-link" href="#">${i}</a>
                            </li>`;
                }

                $('#pagination').html(html);

                // Click vào trang
                $('#pagination .page-link').click(function (e) {
                    e.preventDefault();
                    currentPage = parseInt($(this).text());
                    renderTable(currentPage);
                    renderPagination();
                });
            }
        });
    </script>
}